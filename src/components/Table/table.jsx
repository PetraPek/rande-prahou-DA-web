import React from 'react';
import './table.css';
import { places } from '../../../places';

export const Table = () => {
  return (
    <div className="table">
      {/* <img src={() => places.map((onePlace) => onePlace)} /> */}
    </div>
  );
};
